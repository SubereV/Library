spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: nama3k67lnq
    url: jdbc:mysql://localhost:3306/library
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
